export const APP_ROUTES = {
    HOME: '/',
    MOVIES: '/movies',
    RATED: '/rated',
} as const;

export const API_BASE_URL = '/api';

export const API_ROUTES = {
    MOVIES: '/movies',
    GENRES: '/genres',
    IMAGES: '/images',
    LOGOS: '/logos',
} as const;
