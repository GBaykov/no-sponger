import React from 'react';
import './index.css';
import { FilterForm } from '../../components/filter-form';

export const MainPage = () => {
  return (
    <main className="main">
      <FilterForm />
    </main>
  );
};
